Run `pip install -r requirements.txt`, if it's the first time running the script
Update `ip.py` if needed
Run `python worker.py`

----------------------------------------------------------------------------------------
If the clipboard changes on virtual machine, poe opens on physical machine
Clipboard is automatically transferred to physical machine through virtual box settings
The clipboard is pasted and sent in the chat
----------------------------------------------------------------------------------------
